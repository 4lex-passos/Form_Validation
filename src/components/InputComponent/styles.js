import styled from "styled-components";

export const Container = styled.div`
    width: 100%;
    padding: 0.7rem 0rem 0.7rem 0rem;
    margin-top: 1rem;
    input {
        border: none;
        border-bottom: 0.1rem solid var(--cor-red-dark);
        padding: 0.7rem 0rem 0.7rem 0rem;
        width: 100%;
    }
`;

export const Label = styled.label`
    color: red;
    font-size: 1.2rem;
`;
